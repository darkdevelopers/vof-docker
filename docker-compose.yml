version: "3"
services:
  mariadb:
    image: mariadb:latest
    env_file:
      - ./config/mariadb/enviroment.env
    volumes:
      - ./data/mariadb:/var/lib/mysql
    ports:
      - "3306:3306"
    restart: always

